{% import 'macros.njk' as macros with context %}

<!DOCTYPE html>
<html lang="de">
    <head>
        <meta http-equiv="refresh" content="0; url=/de" />
        <title>{{title}}</title>
    </head>
    <body>
        {{ content | safe }}
    </body>
</html>